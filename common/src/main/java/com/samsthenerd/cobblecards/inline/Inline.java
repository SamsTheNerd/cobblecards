package com.samsthenerd.cobblecards.inline;

import java.util.HashMap;
import java.util.Map;

import net.minecraft.util.Identifier;

// this will probably be bumped out into its own mod Soon, but i want to get it working in this test environment first
public class Inline {
    public static final Map<Identifier, InlineRenderer> RENDERERS = new HashMap<>();

    public static void addRenderer(Identifier id, InlineRenderer renderer){
        
    }
}
